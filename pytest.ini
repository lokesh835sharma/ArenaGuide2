[pytest]
# Tests are fully offline and deterministic (MockLLM, in-memory fixtures).
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing
filterwarnings =
    ignore::DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient`:UserWarning
